"""Not implemented yet — structural placeholder only.

When eBay support is built, this holds pure payload-building for eBay's
Inventory API (offer/listing JSON), mirroring
integrations/marketplaces/baselinker/mapper.py: no network calls, no DB
lookups, so it stays independently unit-testable from client.py.
"""
